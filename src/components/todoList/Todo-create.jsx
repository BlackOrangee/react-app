import React from 'react';

const TodoCreate = () => {
    return (
        <div>
            <input type="text" />
            <button>Add</button>
        </div>
    );
}

export default TodoCreate;
